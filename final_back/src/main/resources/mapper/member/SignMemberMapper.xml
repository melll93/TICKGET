<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="back.spring.final_back.member.repository.SignUpDao">
	<select id="login" parameterType="map" resultType="string">
				select member_name from member230324
		<where>
			<if test='member_id!=null and member_id.length()>0'>
				AND member_id = #{memberId}
			</if>
			<if test='member_pw!=null and member_pw.length()>0'>
				AND member_pw = #{memberPw}
			</if>
		</where>
	</select>

	<insert id="memberInsert" parameterType="map">
		INSERT INTO member(
		<!-- member_no, -->
		member_domain,
		member_id,
		member_password,
		member_name,
		member_age,
		member_birth,
		member_email,
		member_gender,
		member_mobile,
		member_nickname,
		member_profile_image,
		member_register_date)
		VALUES(
		<!-- <if test="memberNo!= null">
			#{memberNo}
		</if> -->
		<if test="memberDomain!= null">
			#{memberDomain}
		</if>
		<if test="memberId!= null">
			,#{memberId}
		</if>
		<if test="memberPassword!= null">
			,#{memberPassword}
		</if>
		<if test="memberName!= null">
			,#{memberName}
		</if>
		<if test="memberAge!= null">
			,#{memberAge}
		</if>
		<if test="memberBirth!= null">
			,#{memberBirth}
		</if>
		<if test="memberEmail!= null">
			,#{memberEmail}
		</if>
		<if test="memberGender!= null">
			,#{memberGender}
		</if>
		<if test="memberMobile!= null">
			,#{memberMobile}
		</if>
		<if test="memberNickname!= null">
			,#{memberNickname}
		</if>
		<if test="memberProfileImage!= null">
			,#{memberProfileImage}
		</if>
		<if test="memberRegisterDate!= null">
			,#{memberRegisterDate}
		</if>
		)
	</insert>
	<select id="memberList" parameterType="java.util.HashMap" resultType="map">
    select
           member_no,
           member_domain,
           member_id,
           member_password,
           member_name,
           member_age,
           member_birth,
           member_email,
           member_gender,
           member_mobile,
           member_nickname,
           member_profile_image,
           member_register_date
      from member
</select >
</mapper>