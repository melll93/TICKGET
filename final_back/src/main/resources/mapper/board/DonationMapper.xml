<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="back.spring.final_back.board.repository.DonationDao">

<insert id="don_boardInsert" parameterType="map">
insert into board_market (board_mk_title, board_mk_pw, board_mk_content, board_mk_date, board_mk_hit, 
                          don_ticket_date, don_ticket_seat, don_ticket_price, don_ticket_count, mem_name)
VALUES (#{boardMkTitle},#{boardMkPw},#{boardMkContent},#{boardMkDate},#{boardMkHit},
        #{donTicketDate}, #{donTicketSeat}, #{donTicketPrice}, #{donTicketCount}, #{memName})
</insert>

<select id="don_boardList" resultType="DonationDto">
select board_mk_no, board_mk_title, don_ticket_count, don_ticket_price, board_mk_date, mem_name, board_mk_hit from board_market
</select>

<update id="don_boardUpdate" parameterType="DonationDto">
 update board_market
 set don_title = #{boardMkTitle},
     don_ticket_date = #{donTicketDate},
     don_ticket_seat = #{donTicketSeat},
     don_ticket_price = #{donTicketPrice},
     don_ticket_count = #{donTicketCount},
     don_content = #{boardMkContent}
<where>
board_mk_pw = #{boardMkPw}
</where>
</update>

<delete id="don_boardDelete" parameterType="int">
delete from board_market
<where>
board_mk_pw = #{boardMkPw}
</where>
</delete>


</mapper>